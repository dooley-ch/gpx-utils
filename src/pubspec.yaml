name: gpx-utils
description: A utility to merge and split gpx files for use with komoot.
version: 1.0.0
homepage: https://github.com/dooley-ch
repository: https://github.com/dooley-ch/gpx-utils

platforms:
  linux:
  macos:
  windows:

publish_to: none

funding:
  - https://www.buymeacoffee.com/dooley-ch

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  toml: ^0.14.0
  path: ^1.8.0
  meta: ^1.9.0
  args: ^2.4.0
  xml: ^6.2.2
  console: ^4.1.0
  version: ^3.0.2
  logging: ^1.1.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
